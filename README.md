# TripAdvisor-Clone
The project is to create a responsive Trip Advisor clone using HTML and CSS.  The goal is to master Flexbox and CSS Grid to build the layout, design reusable listing cards, and use Media Queries to ensure the site adapts perfectly across all devices.
